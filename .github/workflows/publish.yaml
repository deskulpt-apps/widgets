name: Publish

on:
  push:
    branches:
      - main

env:
  GH_TOKEN: ${{ github.token }}
  HEAD_SHA: ${{ github.sha }}
  PUBLISH_PLAN_PATH: "publish-plan.json"
  API_VERSION: 2026-01-10

concurrency:
  group: publish
  cancel-in-progress: false

jobs:
  validate-and-plan:
    runs-on: ubuntu-latest
    outputs:
      HAS_PUBLISH_PLAN: ${{ steps.validate-changes.outputs.HAS_PUBLISH_PLAN }}
      NEEDS_PUBLISH_WIDGETS: ${{ steps.validate-changes.outputs.NEEDS_PUBLISH_WIDGETS }}
      NEEDS_PUBLISH_PLUGINS: ${{ steps.validate-changes.outputs.NEEDS_PUBLISH_PLUGINS }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Read API checkpoint
        shell: bash
        run: |
          set -euo pipefail
          API_DIR="$RUNNER_TEMP/api-wt"
          git fetch origin api:refs/remotes/origin/api --depth=1
          git worktree add "$API_DIR" origin/api
          API_CHECKPOINT=$(cat "$API_DIR/.checkpoint" | tr -d '\r\n')
          BASE_SHA=$(git merge-base "$API_CHECKPOINT" "$HEAD_SHA")
          echo "BASE_SHA=$BASE_SHA" >> $GITHUB_ENV
          git worktree remove -f "$API_DIR" || true

      - name: Install pnpm
        uses: pnpm/action-setup@v5

      - name: Node setup
        uses: actions/setup-node@v6
        with:
          node-version-file: .node-version
          cache: "pnpm"

      - name: Install node dependencies
        run: pnpm install --frozen-lockfile

      - name: Ruby setup
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.4"
          bundler-cache: true

      - name: Install oras
        uses: oras-project/setup-oras@v2

      - name: Find changed files and publishers
        shell: bash
        run: |
          set -euo pipefail
          DIFF_FILES=$(git diff --name-only --no-renames "$BASE_SHA" "$HEAD_SHA" | grep -E '^(publishers|widgets|plugins)/.*\.yaml$' || true)
          if [ -z "$DIFF_FILES" ]; then
            echo "No matching files changed."
            echo "CHANGED_PUBLISHERS=" >> $GITHUB_ENV
            exit 0
          fi
          CHANGED_PUBLISHERS=$(echo "$DIFF_FILES" | xargs -n 1 basename -s .yaml | sort -u | xargs)
          echo "CHANGED_PUBLISHERS=$CHANGED_PUBLISHERS" >> $GITHUB_ENV

      - name: Validate changes
        id: validate-changes
        shell: bash
        env:
          LICENSEE_DETECT_SCRIPT: bundle exec licensee detect "$1" --json
        run: |
          pnpm validate-changes

          HAS_PUBLISH_PLAN=0
          NEEDS_PUBLISH_WIDGETS=0
          NEEDS_PUBLISH_PLUGINS=0
          if [ -s "$PUBLISH_PLAN_PATH" ]; then
            HAS_PUBLISH_PLAN=1
            NEEDS_PUBLISH_WIDGETS=$(jq 'if any(.[]; .collection == "widgets") then 1 else 0 end' "$PUBLISH_PLAN_PATH")
            NEEDS_PUBLISH_PLUGINS=$(jq 'if any(.[]; .collection == "plugins") then 1 else 0 end' "$PUBLISH_PLAN_PATH")
          fi

          echo "HAS_PUBLISH_PLAN=$HAS_PUBLISH_PLAN" >> $GITHUB_OUTPUT
          echo "NEEDS_PUBLISH_WIDGETS=$NEEDS_PUBLISH_WIDGETS" >> $GITHUB_OUTPUT
          echo "NEEDS_PUBLISH_PLUGINS=$NEEDS_PUBLISH_PLUGINS" >> $GITHUB_OUTPUT

      - name: Upload publish plan
        if: ${{ steps.validate-changes.outputs.HAS_PUBLISH_PLAN == '1' }}
        uses: actions/upload-artifact@v7
        with:
          name: publish-plan
          path: ${{ env.PUBLISH_PLAN_PATH }}
          if-no-files-found: error

  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write
      id-token: write
      attestations: write
      artifact-metadata: write
    needs: validate-and-plan
    if: ${{ needs.validate-and-plan.outputs.HAS_PUBLISH_PLAN == '1' }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Install pnpm
        uses: pnpm/action-setup@v5

      - name: Node setup
        uses: actions/setup-node@v6
        with:
          node-version: lts/*
          cache: "pnpm"

      - name: Install node dependencies
        run: pnpm install --frozen-lockfile

      - name: Install oras
        uses: oras-project/setup-oras@v2

      - name: Login to GHCR
        shell: bash
        run: |
          echo "$GH_TOKEN" | oras login ghcr.io -u "${{ github.actor }}" --password-stdin

      - name: Download publish plan
        uses: actions/download-artifact@v8
        with:
          name: publish-plan
          path: .

      - name: Prepare API worktree
        shell: bash
        run: |
          set -euo pipefail
          API_DIR="$RUNNER_TEMP/api-wt"
          git fetch origin api:refs/remotes/origin/api --depth=1
          git worktree add "$API_DIR" origin/api
          echo "API_DIR=$API_DIR" >> $GITHUB_ENV

      - name: Publish widgets
        shell: bash
        if: ${{ needs.validate-and-plan.outputs.NEEDS_PUBLISH_WIDGETS == '1' }}
        run: pnpm publish-widgets

      - name: Commit API updates
        shell: bash
        run: |
          set -euo pipefail
          gh auth setup-git
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          cd "$API_DIR"
          echo "$HEAD_SHA" > .checkpoint
          git add -A
          git commit -m "publish: $HEAD_SHA"

          COMMIT_SHA=$(git rev-parse HEAD)
          COMMIT_TIME=$(git show -s --format=%cI "$COMMIT_SHA")
          HEADS_FILE="heads/$API_VERSION.json"
          jq -n -c --arg sha "$COMMIT_SHA" --arg time "$COMMIT_TIME" '{updatedAt: $time, rev: $sha}' > "$HEADS_FILE"
          git add "$HEADS_FILE"
          git commit -m "publish: point $API_VERSION to $COMMIT_SHA"

          git push origin HEAD:api
